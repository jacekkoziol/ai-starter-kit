---
name: TODO-skill-name
description: One line — what this does and the trigger that should invoke it.
---

# {Skill name}

> **Before copying,** confirm no existing skill already covers this task (see
> [When to add one](../README.md)) and extend that one if it does. Then copy this file to
> `skills/{name}/SKILL.md`, set the frontmatter `name:` to that same `{name}` — the folder name *is*
> the invocable command, so the two must match — and fill it in. Finally, add a row to
> [the skills index](../README.md). (The relative links below are written for that copied location —
> `skills/{name}/SKILL.md` — so they resolve once copied.)

## When to use

- TODO — the trigger(s). Be specific so the agent picks it at the right moment.

## Read first

- TODO — the [reference](../../reference/) doc(s) this assumes are already in context.

## Procedure

1. TODO — step (what to do, where, what to copy).
2. TODO — step.
3. TODO — step.

## Verify

- [ ] TODO — how to confirm it worked (the build/test/lint command from `AI/PROJECT.md`).

## Anti-patterns

- ❌ TODO — the mistake this skill exists to prevent.
